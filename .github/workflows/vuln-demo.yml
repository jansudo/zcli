name: Vulnerable Demo - Script Injection PoC
on:
  workflow_dispatch:
    inputs:
      release-type:
        description: Release type string
        required: true
        type: string

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Vulnerable command execution
        run: |
          echo "=== Executing with input ==="
          echo "Input value: ${{ inputs.release-type }}"
          echo "=== End ==="

